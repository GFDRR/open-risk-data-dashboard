# -*- coding: utf-8 -*-
# Generated by Django 1.11.1 on 2018-02-23 12:55
from __future__ import unicode_literals

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('ordd_api', '0015_dataset_score_and_score_th_norm'),
    ]

    operations = [
        migrations.AlterField(
            model_name='dataset',
            name='is_bulk_avail',
            field=models.BooleanField(help_text='Data is available in bulk if the whole dataset can be downloaded or accessed easily. Conversely it is considered non-bulk if the citizens are limited to just getting parts of the dataset (for example, if restricted to querying a web form and retrieving a few results at a time from a very large database).', verbose_name='Is the data available in bulk?'),
        ),
        migrations.AlterField(
            model_name='dataset',
            name='is_digital_form',
            field=models.BooleanField(help_text='This question addresses whether the data is in digital form (stored on computers or digital storage) or if it only in paper form.', verbose_name='Is data in digital form?'),
        ),
        migrations.AlterField(
            model_name='dataset',
            name='is_existing',
            field=models.BooleanField(help_text='Is the data collected at all? The data can be in any form (paper or digital, offline or online etc). If it is not, then all the other questions are not answered.', verbose_name='Does the data exist?'),
        ),
        migrations.AlterField(
            model_name='dataset',
            name='is_pub_available',
            field=models.BooleanField(help_text='This question addresses whether the data is "public". This does not require it to be freely available, but does require that someone outside of the organization can access it in some form (examples include if the data is available for purchase, if it exists as a PDF on a website that you can access, if you can get it in paper form - then it is public). If a freedom of information request or similar is needed to access the data, it is not considered public.', verbose_name='Publicly available?'),
        ),
    ]
